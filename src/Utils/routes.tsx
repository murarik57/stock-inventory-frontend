const routes = {
  ROOT: "/",
  // app routes start
  APP: "/app",

  DASHBOARD: "/app/dashboard",

  PRODUCT: "/app/product",
  ORDER: "/app/order",
};

Object.freeze(routes);
export default routes;
